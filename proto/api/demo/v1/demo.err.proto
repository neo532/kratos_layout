syntax = "proto3";

package demo.v1;

import "errors/errors.proto";

option go_package = "api/demo/v1;v1";

enum ErrorReason {
    // default
    option (errors.default_code) = 500;

    DEMO_NEED_LOGIN = 0 [(errors.code) = 403];
}
